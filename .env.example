# Security Environment Configuration for NextStep CodeArena

# CRITICAL SECURITY NOTE: 
# API keys have been moved to backend for security!
# Judge0 API key should NOT be exposed in frontend environment variables.

# Backend API Configuration
# Set this to your secure backend server URL
VITE_BACKEND_URL=http://localhost:3001

# Firebase Configuration (these can be public)
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=your-app-id

# Security Settings
VITE_ENABLE_SECURITY_MODE=true
VITE_RATE_LIMIT_ENABLED=true
VITE_MAX_CODE_LENGTH=50000
VITE_SESSION_TIMEOUT=3600

# Development vs Production
NODE_ENV=development

# =================================
# BACKEND-ONLY ENVIRONMENT VARIABLES
# =================================
# These should ONLY be set in your backend server environment
# NEVER prefix with VITE_ (which exposes them to the frontend)

# Judge0 API Configuration (BACKEND ONLY)
# JUDGE0_API_KEY=your-judge0-rapidapi-key
# JUDGE0_HOST=judge0-ce.p.rapidapi.com

# Firebase Admin SDK (BACKEND ONLY)
# FIREBASE_ADMIN_PROJECT_ID=your-project-id
# FIREBASE_ADMIN_CLIENT_EMAIL=your-service-account-email
# FIREBASE_ADMIN_PRIVATE_KEY=your-private-key

# Database Configuration (BACKEND ONLY)
# DATABASE_URL=your-database-url
# REDIS_URL=your-redis-url

# Security Configuration (BACKEND ONLY)
# ENCRYPTION_KEY=your-32-character-encryption-key
# JWT_SECRET=your-jwt-secret-key
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100

# Copy this file to .env.local and add your actual values
# Never commit real API keys to version control!
